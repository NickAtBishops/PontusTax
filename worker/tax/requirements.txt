# Engine
openpyxl>=3.1
httpx>=0.27
beautifulsoup4>=4.12
pypdf>=4.0
python-dotenv>=1.0

# Skyvern cloud SDK (lightweight — remote API only)
skyvern>=1.0

# Optional Claude adjudication layer
anthropic>=0.40

# Firebase / GCP (Firestore mode; unused with --local-xlsx)
google-cloud-firestore>=2.16
google-cloud-storage>=2.16
