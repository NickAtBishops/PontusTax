# Pinned for reproducibility. Bump these intentionally, not as a
# side-effect of `pip install -U`.
fastapi==0.115.6
uvicorn[standard]==0.32.1
openpyxl==3.1.5
python-multipart==0.0.19

# Dev-only (test runner). Cloud Run images don't need it but it doesn't
# bloat the image meaningfully and keeps the local-dev install one
# command. Move to a requirements-dev.txt if image size matters.
pytest==8.3.4
httpx==0.28.1
